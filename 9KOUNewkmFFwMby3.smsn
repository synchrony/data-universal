@id 9KOUNewkmFFwMby3
@title linear regression
@alias http://dbpedia.org/resource/Linear_regression
@created 1496711234769
@text ```
# 'vars' is a data frame

regr.fit(
   vars.iloc[:,:-1] # all but the last column (2d, even if |columns|=1)
  ,vars.iloc[:,-1]  # the last column, as a series (1d)
)
```
